import 'package:flutter/material.dart';

class DevicesWidget extends StatelessWidget {
  const DevicesWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Devices Management'),
    );
  }
}